"""Vim Python Extensions (VPE) support library.

This is a pure Python library, thhat can be used outside of the Vim's Python3
interpreter.

It is not necessarily of much use on its own, but it keeps the VPE code base
smaller, separates concerns and this code is much easier to test.
"""
